# Politica Git

- Inspeccionar `git status`, `git diff` y `git log` esta permitido.
- No cambiar de rama, crear commits, hacer merge, rebase, reset o push sin
  autorizacion explicita.
- Nunca ocultar cambios existentes del usuario.
- Antes y despues de editar, reportar el conjunto de archivos afectados.
